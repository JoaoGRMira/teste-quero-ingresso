import { 
    print, 
    caretDown
} from '../../images/icons';

const FilterButtons = ({ filterHandler, printHandler }) => {
    return (
        <button class="btn btn-primary" type="submit">Button</button>
    )
}

export default FilterButtons;